[pytest]
testpaths = backend/tests
python_files = test_*.py
addopts = -v --cov=backend --cov-report=term-missing
asyncio_mode = auto
env =
    DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/gfg_test
    TESTING=1
